# 🔐 Py Password Generator

A simple Python script that generates a secure random password based on user input.  
This mini–project is beginner-friendly and demonstrates the use of loops, lists, random selection, and string manipulation in Python.

---

## 🚀 Features

- Generates random **letters**, **numbers**, and **symbols**
- Fully customizable password structure  
- Randomly shuffles characters for stronger security  
- Beginner-friendly code  
- Only standard Python library (`random`) used  

---

## 🧠 How It Works

The script:

1. Asks the user how many letters, numbers, and symbols they want
2. Chooses random characters using `random.choice()`
3. Stores them in a list
4. Shuffles the list with `random.shuffle()`
5. Converts the list into a final password string

---
